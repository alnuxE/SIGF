FROM node:20-alpine

WORKDIR /app

# Instala dependencias primero (mejor cache)
COPY package*.json ./
RUN npm install

# El código real se monta como volumen en docker-compose (hot-reload)
COPY . .

EXPOSE 4000

CMD ["npm", "run", "dev"]
